"""PR•VISION — multimodal verification package.

Submodules:
    ingestion  — content acquisition, type detection, normalization, provenance
    claims     — claim extraction, typing, entity/time context
"""
